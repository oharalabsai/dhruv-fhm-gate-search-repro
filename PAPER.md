# dhruv-fhm-gate-search — campaign report

**Full paper: [paper/main.pdf](paper/main.pdf)** (LaTeX source: paper/main.tex)

Cycles: 4 · Experiments: 4 · Promotions: 1 · Final incumbent score: 54.0

> Draft artifact. Before this leaves the building it must pass docs/paper-audit-protocol.md; human-only byline + AI-contribution statement.

## Abstract

Reducing the count of two-qubit entangling gates is the dominant lever for running a quantum circuit within the coherence budget of near-term trapped-ion hardware. We report a methodological replication rather than a discovery. An integrity-gated, pre-registered autonomous propose-score-iterate loop -- wrapped in a blinded verifier and a strict promotion ratchet -- is pointed at one first-order Trotter step of the N=6 Fermi-Hubbard model written as a Z2 lattice gauge theory (18 qubits) and compiled to the IonQ Aria native gate set {GPi, GPi2, RZ, MS} (arXiv:2411.07778). A candidate is scored by its Molmer-Sorensen (MS) gate count and is admitted only if its state-averaged mean infidelity against the ideal Trotter-step unitary is at most 1e-4 on a hidden battery of 48 random input states; otherwise it is invalid and cannot be promoted. Following a human-specified per-block resynthesis recipe supplied in the campaign's public organs, the loop reached a valid 54-MS circuit at mean infidelity 2.38e-13 in a single promotion (EXP-0001): the blinded gate certified that the harness reproduces the hand-designed 9N=54 endpoint at machine precision -- a 47% reduction against our 102-MS baseline. We are explicit that this is verification of a human-specified strategy, not autonomous rediscovery, and we disclose the seeding symmetrically: both the win recipe (via the frozen contract) and the four attack lanes for going below 54 (via the campaign direction) were supplied by a human director. Within those lanes the loop, unaided, instantiated each lane as a runnable circuit construction, wrote a falsifier and kill-threshold for each, generated a self-priced chain of reopening conditions, made the offline feasibility determinations that decided when not to submit, and extracted the closing physical mechanism (MS entangling power is conserved, not fungible). Three sub-54 attempts failed: one 53-MS circuit was submitted and the blinded scorekeeper certified it invalid at mean infidelity 0.313 (EXP-0003) -- the harness catching a plausible over-pruning, the failure mode raised against the learned-Hamiltonian experiment of Jafferis et al. (2022); the other two were determined infeasible in the loop's own offline analysis and resubmitted the incumbent (EXP-0002, EXP-0004). The search met its kill criterion after three consecutive non-promotions. Within this ruler and fidelity bar, 54 MS is a firm bound, and we do not claim the hand design is suboptimal. The contribution is method and integrity: a system that reproduces and self-certifies a human optimization at machine precision, refuses to fool itself, and reaches an honest null.

## Belief state at close

# PRIORS — dhruv-fhm-gate-search

Belief state. Every claim: `[STATUS] claim (evidence)`. Wrong beliefs get status-flipped in place, never deleted.

- [ALIVE] baseline scores 102.0000 on the hidden battery (EXP-0000)
- [ALIVE] The whole Trotter step compiles to exactly 54 MS gates at infidelity <= 1e-4 by resynthesizing each block to its known per-block MS minimum: fit ONE canonical 3-qubit hopping block (HOP_UNITARY) to a 4-MS native ansatz, replicate it to all 12 identical hopping blocks by qubit relabelling, and realize each of the 6 on-site blocks (INT_UNITARY) as a single MS gate. Plain version: Dhruv's ideal step is built from 12 copies of the same little 3-qubit operation plus 6 tiny 2-qubit ones; if we hand-optimize the 3-qubit one down to 4 entangling gates ONCE and stamp it everywhere, plus 1 gate each for the small ones, we get 12x4 + 6x1 = 54 entangling gates -- exactly matching his hand-designed 9N=54, and far below our 102 baseline. (Δ=-48.0000) — mechanism: The 102-MS baseline came from a naive CNOT-ladder compilation that synthesized each block as a generic unitary, spending entangling gates to build structure the Hamiltonian already fixes; the win came from exploiting tha (EXP-0001)
- [DEAD] PLAIN: Dhruv's ideal Trotter step is built from 12 copies of the same little 3-qubit "hopping" operation (each needs exactly 4 entangling MS gates to be perfect) plus 6 tiny 2-qubit ones. The incumbent hits 54 MS at essentially perfect fidelity (~1e-13), but the scorer only requires infidelity <= 1e-4 -- roughly ten billion times looser. The bet: the 4th MS gate in a hopping block might carry so little weight that dropping it (using a 3-MS approximation) barely dents fidelity. If the best 3-MS fit of one hopping block has mean infidelity r, then because block errors add roughly linearly, we can afford to prune k = floor(5e-5 / r) of the 12 hopping blocks from 4->3 MS and still stay under the 1e-4 bar, giving a certified circuit of (54 - k) MS -- strictly below his hand-designed 9N=54 (the STRETCH). FORMAL: replace k of the 12 identical 4-MS hopping-block subcircuits with the 3-MS best fit of HOP_UNITARY; on-site blocks stay at 1 MS each; certify the assembled 18-qubit circuit's state-averaged mean infidelity vs U_ideal <= 1e-4 with margin. This is approximate pruning below the exact per-block minimum (Director lane 4), NOT a per-block re-min (that was EXP-0001). (Δ=+0.0000, no improvement) — mechanism: The result (54 MS at infidelity 2.38e-13 — bit-for-bit the exact incumbent, meaning k=0) fired the falsifier: the best 3-MS fit of the hopping block has residual r > 5e-5, so floor(5e-5/r)=0 and not even one block could  {reopen: If refuted (r > 5e-5, k=0), the below-54 direction reopens ONLY when a WHOLE-CIRCUIT (not per-block) angle optimizer over a fixed 53-MS native ansatz demonstrates, in an offline probe on my own random states, mean infidelity <= 1e-4 -- i.e., neighboring blocks' free angles can COMPENSATE one hopping block's missing 4th MS (something per-block pruning structurally cannot do). Priced: requires implementing an 18-qubit statevector angle optimization (scipy L-BFGS over the native gate angles of the 53-MS ansatz), ~1 coder cycle. Retry only if such a probe shows <= 1e-4 is feasible at 53 MS.} (EXP-0002)
- [DEAD] PLAIN: Dhruv's ideal Trotter step is 12 copies of the same 3-qubit 'hopping' operation (each perfect at 4 entangling MS gates) plus 6 tiny 2-qubit ones (1 MS each), total 54. Our incumbent hits exactly 54 at near-perfect fidelity. To beat him we must drop ONE entangling gate (to 53) and still land within the scorer's fidelity tolerance (1e-4). EXP-0002 showed you cannot do this by shrinking ONE block in isolation to 3 MS -- its leftover error is too big. The new bet is compensation: after removing the 4th MS of one hopping block, let the FREE ANGLES of the neighboring blocks that share qubits with it soak up the resulting error. Because those neighbors touch the same qubits, a whole-circuit angle re-optimization can push the total error back down in a way per-block fitting structurally cannot. FORMAL: build the exact 54-MS incumbent from target primitives; reduce exactly one hopping block b* from its 4-MS realization to a 3-MS ansatz (warm-started from fit_block(HOP_UNITARY, hop_qubits(b*), n_ms=3)); then jointly re-optimize, via scipy L-BFGS, the native angles of b*'s 3-MS ansatz AND of every block whose qubit support overlaps b* (all disjoint blocks frozen), minimizing state-averaged mean infidelity vs U_ideal. If the global optimum reaches <= 1e-4 with margin, ship the resulting 53-MS circuit. This is Director lane 2/3 (whole-circuit angle re-optimization exploiting cross-block qubit sharing), NOT per-block pruning (EXP-0002), and it is exactly the whole-circuit-compensation probe that EXP-0002's reopen condition priced open. — REJECTED, integrity: INVALID: INVALID: infidelity 3.13e-01 > 1e-04 (MS=53) — mechanism: The 4th MS gate carries irreducible two-qubit entangling content, and that content cannot be manufactured by the free single-qubit angles (GPi/GPi2/RZ) of neighboring blocks — MS entangling power is conserved, not fungib {reopen: If this dies (best global-optimized 53-MS ansatz stays > 1e-4 on the fresh draw, or scorekeeper certifies INVALID/no-promotion), the below-54 direction reopens ONLY via a genuinely STRUCTURAL cross-block saving, not approximate pruning: identify two hopping blocks that are CONSECUTIVE in the frozen application order AND share >=1 qubit (so their ordered product on the combined 5-qubit support is exact with no intervening block on that support), form that joint 5-qubit unitary, and demonstrate in an offline fit_block(U_joint, joint_qubits, n_ms=7) probe on my own random states that its residual mean infidelity <= 1e-4 -- i.e. a qubit-sharing block PAIR compresses 8 MS -> 7 MS exactly. Priced: requires enumerating the frozen block order, finding a qualifying consecutive overlapping pair, and one 5-qubit fit, ~1 coder cycle. Retry below-54 only if such a probe shows <= 1e-4 feasible at the pair level.} (EXP-0003)
- [DEAD] PLAIN: Dhruv's ideal Trotter step is 12 copies of the same 3-qubit 'hopping' operation (each perfect at 4 entangling MS gates) plus 6 tiny 2-qubit on-site ones (1 MS each), total 54 -- which our incumbent already hits exactly. EXP-0002 (prune one block 4->3 in isolation) and EXP-0003 (drop a 4th MS and let neighbors' single-qubit angles compensate) both died: the 4th MS carries irreducible two-qubit entangling content that single-qubit angles cannot manufacture. The ONLY door left open for beating 54 is a genuinely STRUCTURAL saving: two hopping blocks that are CONSECUTIVE in the frozen application order AND share a qubit are, on their combined 5-qubit support, just one honest 5-qubit unitary -- and a 5-qubit unitary built from two structured hopping blocks may be exactly synthesizable in 7 MS rather than 4+4=8, because the shared qubit lets entangling content be reused across the seam instead of paid for twice. If ONE such pair compresses 8 MS -> 7 MS at the fidelity bar, the whole step drops to 53 MS. FORMAL: enumerate the frozen block order in target.ideal_step; find hopping-block pairs (b1,b2) that share >=1 qubit and have NO intervening block touching supp(b1) U supp(b2) between them (so their ordered product on the combined support is exact); form U_joint = the ordered product of the two block unitaries on those 4-or-5 qubits; run target.fit_block(U_joint, joint_qubits, n_ms=7); if the best such fit yields FULL-circuit state-averaged mean infidelity <= 1e-4 with margin when the 7-MS joint subcircuit replaces the two 4-MS subcircuits (all other blocks untouched), ship the resulting 53-MS circuit. This is Director lane 3 (structural cross-block merging), exactly the pair-level 8->7 probe that EXP-0003's reopen condition priced open -- NOT approximate pruning (EXP-0002) and NOT single-qubit-angle compensation (EXP-0003). (Δ=+0.0000, no improvement) — mechanism: The 4-MS-per-block minimum reflects irreducible two-qubit entangling content carried on each hopping block's own 3-qubit support; joining two consecutive blocks at a single shared qubit only stitches their supports into  {reopen: If this dies (no qualifying consecutive qubit-sharing hopping pair achieves full-circuit offline infidelity <= 1e-4 at 53 MS), below-54 reopens ONLY via a strictly LARGER exact structural unit, demonstrated feasible in an offline probe on my own random states BEFORE any submission: either (i) a consecutive, mutually-qubit-sharing hopping TRIPLE (three blocks whose combined support has no intervening block) whose joint unitary fits fit_block(U_joint, joint_qubits, n_ms=11) at residual <= 1e-4 (12 MS -> 11), OR (ii) an adjacent overlapping hopping-block/on-site-block pair (sharing >=1 qubit, no intervening block on their combined support) whose joint unitary compresses its combined MS by exactly 1 at residual <= 1e-4. Priced: enumerate the frozen order for the qualifying multi-block unit and run one joint fit, ~1 coder cycle. If neither probe shows <= 1e-4, the below-54 stretch is exhausted -> consolidate to the honest-null 54-MS bound.} (EXP-0004)

